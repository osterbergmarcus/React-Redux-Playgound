import React from 'react'
import { render } from 'react-dom'
import App from '../components/App'

//render the main component and mount it to the root DOM element
render(
  <App />, document.getElementById('app')
)